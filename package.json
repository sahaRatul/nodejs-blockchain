{
  "name": "blockchain-api",
  "version": "1.0.0",
  "description": "Demo Blockchain App",
  "repository": {
    "type": "git",
    "url": "git://github.com/sahaRatul/nodejs-blockchain.git"
  },
  "main": "index.js",
  "scripts": {
    "start": "rimraf build && npm run lint && babel src --presets env --out-dir build && node build/index ",
    "build": "rimraf build && npm run lint && babel src --presets env --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src"
  },
  "author": "Ratul Saha",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0",
    "body-parser": "^1.18.2",
    "crypto-js": "^3.1.9-1",
    "elliptic": "^6.4.0",
    "express": "^4.16.3",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "rimraf": "^2.6.2"
  }
}
